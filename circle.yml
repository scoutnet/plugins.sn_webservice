## Customize the test machine
machine:

  timezone:
    Europe/Berlin # Set the timezone

  # Add some environment variables
  environment:
    CIRCLE_ENV: test

## Customize test commands
test:
  override:
    - phpunit -c Tests/Builds/UnitTests.xml # use PHPunit for testing
